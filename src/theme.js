import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#86BBD8",
      dark: "#336699",
    },
    secondary: {
      main: "#DAF7DC",
      dark: "#9EE493",
    },
    background: {
      default: "#2f4858",
    },
  },
});

export default theme;
